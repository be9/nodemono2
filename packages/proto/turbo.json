{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "codegen": {
      "inputs": ["buf.gen.yaml", "buf.work.yaml", "protosrc/**"],
      "outputs": ["gen/**"]
    }
  }
}
