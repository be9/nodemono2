{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "inputs": ["buf.gen.yaml", "buf.work.yaml", "protosrc/**"],
      "outputs": ["gen/**"]
    }
  }
}
