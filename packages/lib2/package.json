{
  "name": "@nodemono2/lib2",
  "main": "build/index.js",
  "description": "the library which depends on many packages with binary extensions",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf build tsconfig.tsbuildinfo",
    "watch": "tsc --build --watch --preserveWatchOutput",
    "build:self": "tsc"
  },
  "typings": "build/index.d.ts",
  "dependencies": {
    "aws-crt": "^1.21.1",
    "cpu-features": "^0.0.9",
    "node-sass": "^9.0.0",
    "sqlite3": "^5.1.7",
    "ssh2": "^1.15.0",
    "@types/cpu-features": "^0.0.3",
    "@types/node-sass": "^4.11.7",
    "@types/ssh2": "^1.15.0"
  }
}
